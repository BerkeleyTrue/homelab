---
all:
  children:
    marceline:
      hosts:
        pve.lan:

    omv:
      hosts:
        omv.lan:
          ansible_user: "{{ ssh_user }}"
          ansible_port: "{{ ssh_port }}"

    rancher:
      hosts:
        rancher.lan:
          ansible_user: rancher

    vms:
      children:
        omv:
