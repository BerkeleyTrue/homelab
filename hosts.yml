---
all:
  children:
    marceline:
      hosts:
        pve.lan:

    omv:
      hosts:
        omv.lan:
          ansible_user: "{{ ssh_user }}"

    rancher:
      hosts:
        rancher.lan:
          ansible_user: rancher

    vms:
      children:
        omv:
