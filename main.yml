---
- hosts: homelab
  vars_files:
    - vars/secrets.yml
  roles:
    - name: install-docker
      become: true
      become_user: root
    - name: build-snapraid
      become: true
      become_user: root
    - name: build-fuse
      become: trge
      become_user: root
    - name: setup-snapraid
      become: true
      become_user: root
    - traefik
